Round 180: the panels spend their own markdown — the r178 raw-asterisk class closes at every render site. The residual queue head (Shipwright rows printing raw markdown, owed its own round per rule 7) opened and closed at the CLASS level: shipwright.js swRows concatenated r[0]/r[1] raw and unescaped and swCite went out through textContent, while app.js cRows already spent r[1] through inlineMD — the fix is every member, not the named line. Five sites: swRows both cells through inlineMD (also closes an escaping hole — raw concat had no HTML escape at all), swCite innerHTML=inlineMD (inlineMD escapes before emphasising, so innerHTML carries no more trust than textContent did), cRows now spends the KEY too (ten vessels title their keys — "The grain ship *Isis*"), cCite same, metricRow's provenance cite through inlineMD (no star in metrics today; the guard for the copy written next). Checked and NOT members: pc-rows (generated literals), btText (day narratives starless — measured). Measured before the fix: cite markdown on 28 of 33 vessels, rows on 17, row keys on 10; voyages 61 cites, battles 8, chapters 6, ports 5. One member staged, seen and REVERTED, named per rule 5: the Santa Cruz voyage NAME carries "*Santiago*", and name is hashed for her route phase (app.js:2825) — the datum fix moves her frozen position; owed its own round with a frame check on era-4 frames. NEW audit rule 'an asterisk the renderer cannot spend': sweeps rows/cite/text of all five card collections for stars the bold-then-italic pass cannot consume — the r178 kalba fault as data; 0 convictions today (python first, then live). Audit 33/0 twice. Frames: no opening 64-check (the 80-min budget holds ONE full check and this change could reach many frames, so the full check is the CLOSE; attribution chain written in HANDOFF — r179 opening passed whole 22:46:45, the one commit since was below-fold prose solo-verified 0.000, only loop.log changed after). Close 64-check on final code 23:14:49-23:52:14 PASSED WHOLE, 64/64 ok, 0 movers, 0 BLANK — every changed pixel sits below a panel fold or in a panel no committed frame opens (ship-junk solo 0.000 proved the fold before the run; the r179 "in every committed frame" note overstated visibility — the asterisks showed in scrolled shots, not frames). Rule-1 look: scrolled corbita panel shot (key, values and three-title cite all render as type, no asterisks). Stamp 1788243202.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
