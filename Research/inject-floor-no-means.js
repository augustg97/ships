/* Injection proof for 'speed floor with no means': strip the `by` field from every
   polar floor. All seven floor records must then fire the rule — a floor that does not
   name what drives it is the door the UNDER-PADDLE card fault walked through. */
(APP.vessels.vessels || APP.vessels).forEach(v => {
  if (v.polar && v.polar.floor) delete v.polar.floor.by;
});
